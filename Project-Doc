20-04-2026

1. I installed `npm create vite@latest`.
2. I ran `npm install`.
3. In `src`, I deleted `App.css`.
4. I cleared the `App.jsx` file.
5. I installed the ES7+ extension in VS Code.
6. In `src`, I modified the `App.jsx` file using `rafce`.
7. I cleared `index.css`.
8. I imported icons and images from a YouTube channel.
9. I copied the favicon from the images and placed it in the `public` folder.
10. I updated `index.html` to link the favicon.
11. I installed Tailwind dependencies in the project.
12. Command runned to downalod the tailwind dependencies
    - npm install tailwindcss @tailwindcss/vite
13. After downloading the tailwind, i need to update the code in the `tailwind.config.js` file
    - import tailwindcss from '@tailwindcss/vite'
    - And update the plugins in the `vite.config.js` file
    - plugins: [react(), tailwindcss()],
14. And also imported the "@import "tailwindcss";" in the `index.css` file
15. Installed the googel fonts 
16. Create componenets folder in the src, in the src --> created a file name navbar.jsx
